// MongoDB connection — implement your connection logic here
import mongoose from 'mongoose';

const connectDB = async () => {
  // TODO: Connect to MongoDB using process.env.MONGODB_URI
};

export default connectDB;
